#include <avr/io.h>
#include <util/delay.h>
#include "def.h"

@@@@@@@@@@@@@@@@@@@@

/*
int main(void){
  init();
  while(1){
    block();
    PORTB = PORTB | (1<<1);
    _delay_ms(10000);
    PORTB = PORTB & ~(1<<1);
  }
  while(1);
}
*/
